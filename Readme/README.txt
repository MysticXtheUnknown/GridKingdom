You can change your terminal size by right clicking the window > click properties > layout tab.

You can use the mouse to build buildings.  To enable, run the game.  Right click the terminal.  Click properties, and DISABLE QuickEdit mode (checkbox).  Clicking on the map should bring up the build menu.